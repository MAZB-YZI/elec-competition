# F32C 云台电机驱动测试进度

## 📋 测试状态总览

| 测试项目 | 状态 | 备注 |
|---------|------|------|
| UART0 上位机通信 | ✅ 通过 | PA10/PA11, 115200bps |
| 命令解析 | ✅ 通过 | 17 条命令 |
| YAW 轴电机控制 | ✅ 通过 | 多圈位置闭环 |
| PITCH 轴电机控制 | ✅ 通过 | 多圈位置闭环 |
| 软件零点设置 | ✅ 通过 | ZERO 命令 |
| 永久零点保存 | ✅ 通过 | SAVE 命令 |
| 回到零位 | ✅ 通过 | HOME 命令 |
| STATUS 状态反馈 | ✅ 通过 | 实时位置/速度 |

---

## 🎯 命令使用说明

### 基础控制命令

| 命令 | 格式 | 说明 | 示例 |
|------|------|------|------|
| `ENABLE` | `ENABLE` | 使能两个电机 | `ENABLE` |
| `DISABLE` | `DISABLE` | 失能两个电机 | `DISABLE` |
| `STATUS` | `STATUS` | 查询当前位置和速度 | `STATUS` |
| `SCAN` | `SCAN` | 扫描电机地址 | `SCAN` |

### 位置控制命令

| 命令 | 格式 | 说明 | 示例 |
|------|------|------|------|
| `M1POS` | `M1POS<角度>` | 设置 YAW 轴目标位置（相对零点） | `M1POS90` |
| `M2POS` | `M2POS<角度>` | 设置 PITCH 轴目标位置（相对零点） | `M2POS45` |
| `M1ADD` | `M1ADD<角度>` | YAW 轴增加角度 | `M1ADD10` |
| `M1SUB` | `M1SUB<角度>` | YAW 轴减少角度 | `M1SUB5` |
| `M2ADD` | `M2ADD<角度>` | PITCH 轴增加角度 | `M2ADD20` |
| `M2SUB` | `M2SUB<角度>` | PITCH 轴减少角度 | `M2SUB10` |

### 速度控制命令

| 命令 | 格式 | 说明 | 示例 |
|------|------|------|------|
| `M1SPD` | `M1SPD<速度>` | 设置 YAW 轴速度（RPM） | `M1SPD30` |
| `M2SPD` | `M2SPD<速度>` | 设置 PITCH 轴速度（RPM） | `M2SPD20` |

### 模式控制命令

| 命令 | 格式 | 说明 | 示例 |
|------|------|------|------|
| `M1MODE` | `M1MODE<模式>` | 设置 YAW 轴模式 | `M1MODE1` |
| `M2MODE` | `M2MODE<模式>` | 设置 PITCH 轴模式 | `M2MODE1` |

### 零点控制命令

| 命令 | 格式 | 说明 | 示例 |
|------|------|------|------|
| `ZERO` | `ZERO` | 把**当前位置**设为零点（临时） | `ZERO` |
| `SAVE` | `SAVE` | 设置硬件零点并保存到 EEPROM（永久） | `SAVE` |
| `HOME` | `HOME` | 回到零位（会先使能电机） | `HOME` |

---

## 📝 使用流程

### 1. 赛场设零点流程（推荐）

```
断掉 12V 供电    ← 电机控制器完全断电
手动转到想用的角度
接通 12V 供电    ← 电机控制器重新初始化
等待 5 秒        ← 等待电机控制器初始化完成
上电自动设零点   ← MCU 读取电机当前位置
ENABLE          ← 使能电机
HOME            ← 回到零位
```

**说明**：这是赛场上调零位的推荐方法，不需要发任何命令，上电自动完成。

### 2. 设新零点流程（手掰方式）

```
断掉 12V 供电    ← 电机控制器完全断电
手动转到想用的角度
接通 12V 供电    ← 电机控制器重新初始化
等待 5 秒        ← 等待电机控制器初始化完成
上电自动设零点   ← MCU 读取电机当前位置
ENABLE          ← 使能电机
HOME            ← 回到零位
```

**说明**：与赛场流程相同，必须断电才能重新设零点。

### 3. 回到零位流程

```
ENABLE          ← 使能电机
HOME            ← 回到零位
```

### 4. 永久零位设置流程

```
ENABLE          ← 使能电机
M1POS200        ← YAW轴转到 200°
M2POS23         ← PITCH轴转到 23°
STATUS          ← 确认位置
ZERO            ← 把当前位置设为零点
STATUS          ← 确认：目标=0°,当前=0°
SAVE            ← 保存为永久零点
HOME            ← 回到零位
DISABLE         ← 失能电机
```

### 4. 步进调整流程

```
ENABLE          ← 使能电机
M1ADD10         ← YAW轴 +10°
STATUS          ← 确认位置
M1SUB5          ← YAW轴 -5°
STATUS          ← 确认位置
M2ADD20         ← PITCH轴 +20°
STATUS          ← 确认位置
DISABLE         ← 失能电机
```

---

## ⚠️ 注意事项

1. **使能状态**：位置控制命令需要先使能电机（`ENABLE`）
2. **失能状态设零点**：失能状态下可以设零点（`ZERO`），不需要先使能
3. **等待时间**：`HOME` 命令后需要等待 3-5 秒再发 `STATUS`
4. **零点设置**：`ZERO` 是临时零点（掉电丢失），`SAVE` 是永久零点
5. **角度单位**：所有角度单位为**度（°）**
6. **速度单位**：速度单位为**RPM（转/分钟）**
7. **默认速度**：上电默认速度为 10 RPM
8. **重新设零点必须断电**：电机不断电只失能，RESET 无法更新零点，必须断掉 12V 供电再重新上电

---

## 🔧 常见问题

### Q1: 发送命令没有响应
**A**: 检查串口设置（115200bps, 8N1）和接线（TX/RX是否交叉）

### Q2: 电机不转动
**A**: 先发 `ENABLE` 使能电机，再发位置命令

### Q3: 位置不准确
**A**: 先发 `ZERO` 设零点，再发位置命令

### Q4: 回到零位不准确
**A**: `HOME` 后等待 3-5 秒再发 `STATUS`

### Q5: 重新上电后位置丢失
**A**: 用 `SAVE` 保存为永久零点，而不是 `ZERO`

---

## 📊 测试记录

### 2026/07/10 云台电机驱动测试

**测试项目**：
- UART0 上位机通信
- 命令解析
- YAW 轴电机控制
- PITCH 轴电机控制
- 软件零点设置
- 永久零点保存
- 回到零位

**测试结果**：
- ✅ 所有基础功能正常
- ✅ 零点设置和保存正常
- ✅ 回到零位功能正常

**发现问题**：
- 上电自动设零点偶尔超时（不影响使用）
- HOME 命令需要等待 3-5 秒才能生效

**解决方案**：
- 已优化 HOME 命令，自动使能电机
- 已优化 ZERO 命令，先请求位置反馈再设零点

---

## 📌 接下来测试

### 1. 双轴协调测试
- 同时控制 YAW 和 PITCH 轴
- 测试两个电机同时转动的稳定性

### 2. 速度控制测试
- 测试不同速度下的位置控制精度
- 测试速度变化时的响应

### 3. 模式控制测试
- 测试不同模式下的行为差异
- 测试模式切换的稳定性

### 4. 极限测试
- 测试大角度位置控制
- 测试高速位置控制
- 测试长时间运行稳定性

### 5. 错误处理测试
- 测试通信异常时的行为
- 测试电机堵转时的行为
- 测试超时处理

---

## 📌 测试工具

- **串口助手**：任意串口助手（推荐 SSCOM 或 PuTTY）
- **波特率**：115200bps
- **数据格式**：8N1
- **发送模式**：发送新行（\r\n）

---

## 📌 测试命令速查

```
ENABLE          ← 使能电机
DISABLE         ← 失能电机
STATUS          ← 查询状态
M1POS<角度>     ← YAW轴目标位置
M2POS<角度>     ← PITCH轴目标位置
M1ADD<角度>     ← YAW轴增加角度
M1SUB<角度>     ← YAW轴减少角度
M2ADD<角度>     ← PITCH轴增加角度
M2SUB<角度>     ← PITCH轴减少角度
M1SPD<速度>     ← YAW轴速度
M2SPD<速度>     ← PITCH轴速度
M1MODE<模式>    ← YAW轴模式
M2MODE<模式>    ← PITCH轴模式
ZERO            ← 设零点（临时）
SAVE            ← 保存零点（永久）
HOME            ← 回到零位
```

---

## 📌 代码结构

```
F32C_GIMBAL_MSPM0G3507/
├── empty.c              # 主程序
├── empty.syscfg         # SysConfig配置
├── Control/
│   ├── DataScope_DP.c/h # F32C协议层
│   ├── uart_callback.c/h # UART3电机通信
│   ├── uart_debug.c/h   # UART0上位机通信
│   ├── control.c/h      # 定时器控制
│   └── show.c/h         # OLED显示
├── drivers/
│   ├── oled.c/h         # OLED驱动
│   ├── key.c/h          # 按键驱动
│   ├── led.c/h          # LED驱动
│   ├── delay.c/h        # 延时函数
│   └── board.h          # 板级定义
└── targetConfigs/
```

---

## 📌 版本记录

- V1.0 - 基础电机控制
- V1.1 - 添加上位机命令
- V1.2 - 添加上电零点功能
- V1.3 - 修复角度显示问题
- V1.4 - 修复零点显示问题
- V1.5 - 修复目标位置重置问题
- V1.6 - 添加 HOME 命令，优化 ZERO 命令
- V1.7 - 添加失能状态设零点功能，优化流程
- V1.8 - 添加断电设零点流程，更新文档
